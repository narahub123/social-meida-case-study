export interface UserSignupType {
  [key: string]: string;
}

export interface BirthType {
  month: number;
  date: number;
  year: number;
}

export interface UserSignupValidType {
  [key: string]: boolean;
}
